package player;

public interface PlayerInterface {
    
    int abassaVolume(int valore);
    int alzaVolume(int valore);

    int abassaLuminosita(int valore);
    int alzaLuminosita(int valore);
}
